# 2026-09-19

## 知识库日报:LLM Wiki 维护 + 概念雷达(自动化第 4 次执行)

### 执行环境
- 环境真实日期 **2026-09-19**(bash `date` 与 WebSearch 返回日期一致),报告与日志按此命名。

### 阶段一:LLM Wiki 维护 —— 全绿,无需修复
- `tools/health.py`:31 页 · Empty/Stub 0 ✅ · Index Sync 0 ✅ · Log Coverage 0 ✅
- 自建 lint(5 维度,33 个 `.md`):悬空 `[[wikilink]]` 0 ✅ · 孤立页 0 ✅(09-17 补的入链未回退)· frontmatter 完整性 0 ✅ · `sources` slug 0 无效 ✅ · index↔磁盘一致性 0 ✅(正向 + 反向)
- 唯一告警仍是**误报**:`log.md` 里的 `[[wikilinks]]` 是叙述性文字;结构文件不计入链接维度
- `raw/` 无新增未摄取材料(6 份 `source_file` 全部可解析)
- **今日未改任何 wiki 正文页**,仅追加 `log.md` 一条维护记录;`overview.md` 判定未过时故未改
- 图谱未重建(输入无变化)——31 节点 / 125 边 / 3 社区

### 阶段二:概念雷达(3 条,均为「缺失的第二根轴」)
| # | 候选 | 缺的是什么 |
|---|---|---|
| 1 ⭐ | **A2A 协议 / Agent2Agent** | 你的协议层只有 [[MCP]](纵向 agent→tool),**缺横向 agent→agent**;2026-08-27 A2A 正式并入 AAIF(与 MCP 同门),官方定调「MCP 垂直 / A2A 水平」;它同时是 09-17 候选 3 与 09-18 候选 2 的底座 |
| 2 | **线索锚定工作记忆 / Cue-Anchored Working Memory** | **直接改写 [[LongTermMemory]] 的核心定义**:「存储是记忆还是文档,由递送机制决定」;实证「主动存 store 114 轮零调用 / harness 注入挺过 138 次压缩」。第二处「新证据挑战既有节点假设」 |
| 3 | **测试时计算 / 推理预算(Test-Time Compute)** | 「有界资源」簇只算了「读进来的 token」,**缺「想出去的 token」这根轴**;推理成本 3~30×、`budget_tokens`/`reasoning_effort` 直接与 [[ContextEngineering]] 的窗口预算博弈 |

- 往期 6 条候选(RAG / Evals / 记忆互操作 / Harness / 子智能体隔离 / 记忆投毒)**均仍未落盘**,本期不重复推荐、并在正文标注**互补而非重复**
- 未入选已备查:**Agent Control Standard**(2026-09-01/02 公布,最鲜活,含 AgBOM + 8 hooks,且明确「委派走 A2A」)、OTel GenAI 语义约定、OWASP 2026 重编号(Excessive Agency #6→#3 / System Prompt Leakage→Hidden Context Exposure)、Agent Skills 生态(40 个 Agent / 17.3 万 star,**可刷新 [[Skill]] 页**)
- 产出:`.workbuddy/memory/concept-radar/2026-09-19.md`(全部来源为 2026-09-19 WebSearch 真实返回,无 `[unverified]` 项;候选 2 的「单作者/小样本」自评已如实标注)

### 经验/坑
- **候选 2 的价值不在「新概念」,而在「既有页面措辞要改」**:它把 [[LongTermMemory]] 的主语从「存储」翻成「递送」。这类信号优先级应高于纯新增——与 09-18 得到的教训一致,现已连续两天出现,说明**当知识网开始成团,新证据更可能落在「修订」而非「新增」上**
- **A2A 与 [[MCP]] 是「同一栈的两层」而非竞争**:写报告时务必把「垂直/水平」「手/同事」这组对照放在最前,否则容易被误读成又一个协议标准
- 每日 lint 的 5 维度已稳定全绿,今日**零修复**;主线工作重心已从「修结构」转到「挖缺口」

---
