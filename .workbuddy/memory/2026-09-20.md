# 2026-09-20

## 知识库日报:LLM Wiki 维护 + 概念雷达(自动化第 5 次执行)

### 执行环境
- 环境真实日期 **2026-09-20**(bash `date` 与 WebSearch 返回日期一致),报告与日志按此命名。

### 阶段一:LLM Wiki 维护 —— 全绿,无需修复
- `tools/health.py`:31 页 · Empty/Stub 0 ✅ · Index Sync 0 ✅ · Log Coverage 0 ✅
- 自建 lint(5 维度,33 个 `.md`):悬空 `[[wikilink]]` 0 ✅ · 孤立页 0 ✅(09-17 补的入链未回退)· frontmatter 完整性 0 ✅ · `sources` slug 0 无效 ✅ · index↔磁盘一致性 0 ✅(正向 + 反向)
- 唯一告警仍是**误报**:`log.md` 里的 `[[wikilinks]]` 是叙述性文字;结构文件不计入链接维度
- `raw/` 无新增未摄取材料(6 份 `source_file` 全部可解析;lint 脚本打印的「6 份未引用」是 Windows 路径分隔符造成的假阳性,已人工逐条确认 6/6 命中)
- **今日未改任何 wiki 正文页**,仅追加 `log.md` 一条维护记录;`overview.md` 判定未过时故未改
- 图谱未重建(输入无变化)——31 节点 / 125 边 / 3 社区

### 阶段二:概念雷达(3 条,全部指向「被当成常量、其实是变量」的黑箱)
| # | 候选 | 缺的是什么 |
|---|---|---|
| 1 ⭐ | **提示缓存 / KV 缓存(Prompt Caching / KV Cache)** | [[Token]] 页写着「Pricing is per-token」——**默认 token 单价是常数**;2026 的实况是**同一 token 三档价**(命中读 0.1× / 未命中 1× / 写入 1.25~2×),且缓存把「稳定内容放前面」从风格偏好升级成**硬约束**(实测命中率 7%→84%、成本 −59%);物理底座正是 [[Transformer]] 的 n² → 缓存放 O(n) |
| 2 | **工具调用 / 结构化输出(Tool Use / Function Calling)** | [[Agent]] 页的 `tool_call = model.choose_tool()` 是**零成本黑箱**;[[ReAct]] 管「怎么想」、[[MCP]] 管「怎么连」、[[Skill]] 管「装什么」,**没人讲「意图怎么变成可校验的调用」**;而这是生产事故头号来源(「想对了、格式写错」) |
| 3 | **提示词自动优化 / GEPA(Prompt Optimization)** | 你的知识网默认**提示词的作者是人**(Skill / ProgressiveDisclosure / ContextEngineering 全靠人手写);GEPA(ICLR 2026 Oral)把 prompt 变成**可搜索优化的变量**,「作者」从人扩成「人 + 优化器」 |

- 往期 9 条候选(RAG / Evals / 记忆互操作 / Harness / 子智能体隔离 / 记忆投毒 / A2A / 线索锚定记忆 / 测试时计算)**均仍未落盘**,本期不重复推荐、并在正文标注**互补而非重复**
- 未入选已备查:**Prompt Injection**(OWASP LLM01:2026 + 新增 OWASP Top 10 for Agentic Applications——但属 09-18 候选 3 的上位,真缺口角度是「对抗性输入」而非风险清单)、**Goal Anchoring / Harness 四机制**(同族 09-18 候选 1)、**「40% dumb zone」阈值**(可直接刷新 [[ContextRot]])、**Agent-controlled 压缩**(主语从 harness 移向 agent)、**LLMLingua**(机械压缩)、**A-RAG**(RAG 的 2026 新形态)、**CDLC**(context supply chain)、**Context Economics** 框定语
- 产出:`.workbuddy/memory/concept-radar/2026-09-20.md`(全部来源为 2026-09-20 WebSearch 真实返回,无 `[unverified]` 项;候选 1 的 OpenAI 写入口径冲突、候选 2 的单来源数字已如实标注)

### 经验/坑
- **「被当成常量的变量」是本日三条的共同结构**:token 的价格、工具调用的契约、提示词的作者——你的页面都把这三样当成了理所当然、无需解释的黑箱。**连续三天出现「修订型信号」(09-18 子智能体隔离、09-19 线索锚定记忆、09-20 价格轴),进一步印证:知识网成团后,新证据更可能落在「既有页面措辞要改」而非「新增节点」上。**
- **候选 1 是最强「修订型」候选**:`Token.md` 白纸黑字「Pricing is per-token」,而缓存把它推翻成「分段计价」——这类「**既有页面写错了**」的信号,优先级应高于纯新增。
- **lint 脚本要注意 Windows 路径分隔符**:`glob` 返回 `\`、frontmatter 里是 `/`,导致 raw 覆盖度检查在 Windows 上**每天都可能假报「N 份未引用」**。以后该维度要么统一 `os.path.normpath`,要么直接人工比对(今日即人工确认 6/6)。

---
