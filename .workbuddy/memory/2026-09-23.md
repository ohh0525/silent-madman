# 2026-09-23

## 知识库日报:LLM Wiki 维护 + 概念雷达(自动化第 8 次执行)

### 执行环境
- 环境真实日期 **2026-09-23**(bash `date` 与 WebSearch 返回日期一致),报告与日志按此命名。

### 阶段一:LLM Wiki 维护 —— 全绿,无需修复
- `tools/health.py`:31 页 · Empty/Stub 0 ✅ · Index Sync 0 ✅ · Log Coverage 0 ✅
- 自建 lint(5 维度,33 个 `.md`):悬空 `[[wikilink]]` 0 ✅ · 未被 index 收录 0 ✅ · frontmatter 完整性 0 ✅ · `sources` slug 0 无效 ✅ · index↔磁盘一致性 0 ✅(正向 + 反向)
- 本期把 `sources/` 页的 `date` + `source_file` 也纳入**必填字段**检查,仍为 0 缺陷
- 唯一告警仍是**误报**:`log.md` / `index.md` / `overview.md` 里的 `[[...]]` 是叙述性文字;结构文件不计入链接维度(连续第 7 天)
- `raw/` 无新增未摄取材料(6 份 `source_file` 全部可解析;最新 mtime 仍是 09-16)
- **今日未改任何 wiki 正文页**,仅追加 `log.md` 一条维护记录;`overview.md` 在阶段一意义上未过时故未改
- 图谱未重建(输入无变化)——31 节点 / 125 边 / 3 社区

### 🟡 重要新事实:09-22 的**三条**候选全部产出 HTML(本次巡检期间提交)
- 提交 `bc022af feat(learning-materials): 新增行动侧三份单概念材料(身份/人在环/工具幻觉)+ 图谱与清单登记`
- 三份文件:`learning-materials/agent-identity.html`、`human-in-the-loop.html`、`tool-hallucination.html` —— 分别对应 09-22 候选 1/2/3(**「信任的行动侧」**)
- 同一次提交还更新了 `README.md`(目录树 + 材料清单表)与 `concept-relationship.md`(新增「**扩展二 · 行动侧**」节,含 Mermaid 子图)
- **这是六期雷达以来第一次有候选走出"已建议"状态,而且一次走完三条**;`雷达 → HTML → 图谱/清单登记` 这条链已被完整验证一次
- ⚠️ **我未摄取、未写任何 wiki 页,也未把它们放进 `raw/`**。三重理由:①它们**不在 `raw/`** 而 Ingest Workflow 的入口就是 `raw/`;②**你自己的 README 给这三份的标注是「待核查」**,即闸门未过;③摄取意味着来源 6→9 + 新增「行动侧」概念页,属「结构性大改动」,按硬约束只建议不执行
- 待你核查后放进 `raw/learning-materials/` 即可触发第 7 次摄取;届时 `overview.md` 需要**一次真正的 synthesis 修订**(而非补注记):本库将首次拥有「**行动侧**」簇,与 entity 侧的 Provenance / CitationLock / Abstention 簇对照 —— 而这两簇的关系图**你已经在 `concept-relationship.md` 里画好了**,wiki 侧补上即可

### 阶段二:概念雷达(3 条,共同结构 =「你的知识网把一切锚在*当轮*,而新证据都发生在*当轮之后*」)
| 时间轴 | 你锚在「当轮」的机制 | 「当轮之后」缺的是什么 |
|---|---|---|
| 一次运行**之内**(崩溃/暂停) | [[LLMContext]] 是当轮预算 | 进度从哪续 —— 候选 1 **持久化执行/Agent 运行时** |
| **摄入之后**(标记过了仍被执行) | [[Abstention]]/[[CitationLock]] 是当轮表态 | 「识别 ≠ 遏制」,最长 46 小时后仍执行 —— 候选 2 **间接提示注入** |
| 任务**之后**(轨迹变能力) | [[Skill]] 是当轮按需加载的知识 | 谁把经验变成能力 —— 候选 3 **从轨迹中学习/自进化** |

| # | 候选 | 缺的是什么 |
|---|---|---|
| 1 ⭐ | **持久化执行 / 智能体运行时(Durable Execution / Agent Runtime)** | [[LongTermMemory]] 管「**知识**的持久化」,**全库无任何一页管「一次运行内**执行状态**的持久化」**。四天内三件事:OpenAI Agents API public beta(09-10,durable session)、Salesforce Agentforce long-horizon runtime(09-11,官方三件套 memory / **durable execution** / dynamic steering)、跨运行时恢复语义对比(证据截止 09-17:Temporal 事件重放 / DBOS 按持久位置采纳 / MS Agent Framework superstep 检查点 / LangGraph 线程快照**四者不可互换**,且**没有任何运行时单凭自己就能让任意外部副作用 exactly-once**)。**缝隙极严**:[[MCP]] 刚在 09-16 因"协议层无状态化"重写——状态被协议交出来后必须有人端住它。**09-22 备查表已点名"建议下一期正式单列"**,本期承接 |
| 2 | **间接提示注入 / 对抗性输入(Indirect Prompt Injection)** | **直接打在 [[Provenance]] 上**:Provenance 假设"来源可标注因而可信任",但模型是 **provenance-blind**(attention 对系统提示/用户输入/外部网页一视同仁,注入指令不带来源元数据)→ 标注来源解决**事后可核查**,不解决**事中不被骗**。arXiv:2609.17320(**Emergence World**,09-15:8 世界×10 agent×16 天、85 万次调用、约 500 亿 token)**"No evaluated world achieved full resilience… Detection did not ensure containment"** —— 识别了威胁仍交互、**写进持久记忆**、**最长 46 小时后**执行;arXiv:2609.22949(09-19,ICML 2026 AIWILD)14 向量、**67% agent 可被越权**、间接注入成功 **43%**、四种架构防御 **31.2%→4.2%**;OWASP **Agent Control Standard v0.1(09-01)** 把执行点定在 **`toolCallRequest`**(副作用**之前**),与 [[Abstention]] 形成"自律 ↔ 他律"对偶 |
| 3 | **从自身轨迹中学习 / 自进化智能体(Self-Improving Agents)** | 你的库认为「知识由**人**写」(Skill / ProgressiveDisclosure / ContextEngineering);2026 的第三条路是 **agent 把自身轨迹蒸馏成准则**,作者=agent 自己。arXiv:2603.10600(四组件 + 三类指引;**AppWorld 留出集 +14.3 pp、复杂任务 +28.5 pp / 相对 +149%**;明确与自己区隔:"既有记忆系统存通用对话事实,本框架理解执行模式"——**这条正好是它与 [[LongTermMemory]] 的边界,且它抽取的学习自带 provenance**);AgentPatterns **Agentic Flywheel**(只分析**失败**轨迹的 boosting;只改 harness 不换模型 **52.8%→66.5%**)。**最该记住的价格标签:回归盲区 —— fix precision 33.7% vs regression precision 11.8%,作者结论是"assume regression blindness"** |

- 往期 **18 条**候选(09-17~09-22:RAG / Evals / 记忆互操作 / Harness / 子智能体隔离 / 记忆投毒 / A2A / 线索锚定记忆 / 测试时计算 / 提示缓存 / 工具调用 / GEPA / 压缩保真度 / 记忆调用时机 / 沙箱 / 身份授权 / HITL 闸门 / 工具幻觉)**均仍未落盘**(除 09-22 候选 1 已出 HTML),本期不重复推荐、正文逐条标注**互补而非重复**
- 备查:**Agent 可观测性 / OTel GenAI 语义约定**(是候选 1 与候选 3 的**共同底座**:ACS 走 OTel+OCSF、轨迹学习走 Langfuse/OTel;截至 2026-09 该约定仍是 **Development 非 stable**;与 09-17 候选 2 Evals 的边界=**离线样本集 vs 生产流量**)、agent 群体越界协作(**受控实验版**,强度高于 09-22 的新闻报道版,建议合并升级)、判别式控制流替代提示(Jev,同一主张三周内第三次出现)、压缩经济学反噬(第三次,并入 09-21 候选 1)、GitHub Copilot 成本/质量三档(**"模型路由"这一概念的产品雏形**)、CVE-2026-62830(Azure SRE Agent CVSS 9.9,agent 身份授权的事故级证据)
- 产出:`.workbuddy/memory/concept-radar/2026-09-23.md`(来源全部为 2026-09-23 WebSearch 真实返回;2 条 arXiv 主来源额外用 WebFetch 复核标题/作者/日期/摘要原文;5 处需自行判断强度的项已逐条标注)

### 经验/坑
- **本期结构是"时间轴"**:前六期的共同结构多落在"空间/层级"上(哪个层缺节点),本期三条的共同点是**同一件事在"当轮之后"的样子**——运行之内、摄入之后、任务之后。**这个角度是新的,而且它解释了一个此前没解释的空白:为什么你的库"当轮"的东西很密(D 维度 15 个概念页),而"跨轮"的东西只有 [[LongTermMemory]] 一个,且它还只管知识不管进度。** 以后找缺口可以试:**把现有节点逐条问"过了这一刻之后呢?"**
- **今日最有价值的一格不是候选,而是"09-22 三条候选一次性全部出 HTML"这件事本身**。它首次证明雷达不是自说自话,而且链路是**批量跑通**的。**建议把"候选落地状态"从二元升级为三态**:⏸ 已建议未动 · 🟡 材料已出待核查 · ✅ 已入 raw 并摄取 —— 否则"未落盘"这个说法会把正在发生的进展盖住(本期已按三态重排表格)。
- **"材料已出但不在 `raw/`"是一个真实的口径空窗,今天必须做判断,我的判断如下(请核对)**:3 份 HTML 既不是"未摄取的新材料"(不在 `raw/`),也不是"已摄取"(不在 `wiki/`)。**我按"不放行"处理,理由是三条同时成立**:①Ingest Workflow 的入口是 `raw/`,`learning-materials/` 是产出区(历史上 5 份已摄取材料**同时存在于两处**,`raw/` 是那份"已投递副本");②**你在 README 里给这三份的标注就是「待核查」**——这是最直接、最不该被忽略的信号;③摄取 3 份来源(6→9)并新增一批行动侧概念页,属「结构性大改动」,按硬约束只建议不执行。**但我要把这个空窗明确说出来:从"你产出 HTML"到"它进 raw/"之间,目前有一段无人认领的流程真空。** 若你希望自动化接手这一步,请在指令里明确授权「learning-materials/ 下新出现的 HTML 视为待摄取并自动投递到 raw/」——**在得到这句授权之前,我默认维持"只提示、不代做"**,因为默认代做就等于绕过你的「人工核查承诺」。**这也是我今天唯一一次在"没做"和"做了"之间做过真实权衡的地方,把它写下来供你校准。**
- **候选 1 的缝隙质量再次显著高于另两条**(与 09-22 同款判据):它能指出**同一份规范的另一半** —— [[MCP]] 页刚写完"协议层无状态",而"状态去哪了"这一问句的答案就是运行时。**能一句话指出"某个刚改过的页面留下的问题"的候选,落地成本最低,应优先排第一。**
- **今天第一次出现"同一工作、两个口径的数字"需要额外一层判断**:arXiv:2603.10600 摘要写"14.3 pp(留出集)/ 28.5 pp(复杂任务)",而二手博文写成"Hard +14.2%"。**处理方式与 09-22 一致:取一手(arXiv 摘要)口径,并在报告里把冲突如实标注、声明采用哪一个。** 另一处更微妙:"ALTK-Evolve 是该论文的产品化"这一关联**只存在于第三方博文**,arXiv 摘要里没有这个产品名 —— **这类"博文自我关联"比数字冲突更需要警惕,因为它不矛盾,只是无据。**
