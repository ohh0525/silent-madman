# 2026-09-16

## 知识库维护自动化上线

### 触发
- 用户要求:把知识库维护工作自动化,每天 13:20 开始整理,并基于对用户的了解发掘当下最有价值的概念

### 建立的自动化
- 名称:知识库日报:LLM Wiki 维护 + 概念雷达
- ID: `567f20a4-9d72-4008-90e0-c83a0df2de89`
- 频率:每天 13:20(cwd 锁定 `D:\桌面\silent-madman`)
- 两个阶段:① LLM Wiki 维护(lint / 摄取新增 raw / 修问题 / 追加 log.md / 更新 overview)
  ② 概念雷达(综合 overview 的 Open Questions + concept-relationship 已有节点 + 近期 daily log + WebSearch 动态 → 输出 1-3 条候选)
- 产出:`.workbuddy/memory/concept-radar/YYYY-MM-DD.md` + 追加 daily log + 本地 commit(绝不 push)
- 硬约束已写入提示:不碰 `raw/`、不碰 token/PAT、不擅自写 `learning-materials/`(受 README 人工核查承诺约束)

## 首轮维护执行结果(手动跑通,验证自动化流程可行)

### lint(用上游官方脚本,比我手写更权威)
- `python tools/health.py` 扫 17 页 → 发现 **1 个真问题**
- 问题:`sources/llm-context.md` 在 log.md 无对应 ingest 记录
- 根因:`health.py:126-154` 的 Log Coverage 检查是「log 条目标题 ↔ 页面 frontmatter title」精确匹配;该源页 title 是 `LLM Context Window(大模型上下文窗口)`,而 log.md / index.md 都用 `LLM Context Window(上下文窗口)`
- 修复:改源页 title 对齐(只动 1 文件;log.md 是 append-only 不动)
- 复跑:Empty/Stub 0 ✅ · Index Sync 0 ✅ · Log Coverage 0 ✅

### 图谱(首次生成)
- `python tools/build_graph.py --no-infer` → **17 节点 / 50 边**
- 产出 `graph/graph.json`(43 KB)+ `graph/graph.html`(62 KB),此前 `graph/` 只有 `.gitkeep`
- 限制:语义推断边需要 LLM 调用(用 `--no-infer` 跳过);`networkx` 未安装 → 社区检测禁用;沙盒无 `pip`(command not found),无法补装

### 自建 lint(补充官方脚本没覆盖的维度)
- 悬空 `[[wikilink]]`:0 ✅
- 孤立页:2 个(BaiduBaike / SourceScore,仅 index.md 可达)——非阻塞
- frontmatter:index.md / log.md 不带(符合上游设计,它们是结构文件)

## 概念雷达产出(3 条候选)

| # | 概念 | 为什么是现在 | 建议动作 |
|---|---|---|---|
| 1 ⭐ | **上下文工程 Context Engineering** | 用户已有 Agent/LLMContext/ProgressiveDisclosure/Skill 四节点,**缺统起来的顶层框架**;Anthropic 2026-07 正式命名 | 生成 `learning-materials/context-engineering.html` |
| 2 | **带溯源的智能体长期记忆** | 上次会话讲过「Agent 记忆系统」但**从未落盘**,且 overview Open Questions 第一条正指向它;论文的「溯源否则弃答」与用户 README 人工核查承诺同源 | 生成 `learning-materials/agent-memory.html` |
| 3 | **MCP 无状态化(2026-07-28 规范)** | wiki 的 `concepts/MCP.md` 已过时;这是 MCP 发布以来最大修订 | **更新 wiki 的 MCP 页**(需下次 ingest + 用户确认),不单开 HTML |

### 关键数据(tool-verified,均来自 WebSearch 真实返回)
- Claude Code 系统提示删掉 **80%+**,编码评测无可测损失
- 工具定义 **77K → 8.7K tokens(−85%)**,Opus 4.5 准确率 **79.5% → 88.1%**
- MCP 代码执行模式 **150K → 2K tokens(−98.7%)**
- Agent Zero Memory:LongMemEval **95.60%** / LoCoMo **93.60%**(arXiv:2608.29606,2026-08-30)
- 8 个 LLM 后端:准确率只差 **3.4 分**,成本差 **约 30 倍**
- 无状态 MCP:Manufact 包体 **−83%**、速度 **+25%**

## 环境/日期校正(值得记)
- 注入的会话时间显示 2026-09-09,但**环境真实日期是 2026-09-16**(bash `date`、python `date.today()`、`git log` 上次提交 09-09、WebSearch 均标 2026-09-16 一致)
- 结论:**以环境时钟为准**,报告与日志按 2026-09-16 命名

## 经验沉淀
- **优先用上游自带工具做维护**:`tools/health.py`(确定性结构检查)、`tools/build_graph.py`(建图,且会自动追加 log)、`tools/heal.py`(补缺失实体页,需 LLM key)。手写检查只作为补充,官方脚本的判定标准才是「合规」的定义
- `health.py` 的 Log Coverage 是**标题精确匹配**(lowercase 后比字符串),所以「同一份资料在不同文件里用不同标题」会直接报错——这是 LLM Wiki 里最容易踩的一致性陷阱
- 沙盒无 `pip`(`pip: command not found`),Python 包依赖(networkx 等)无法安装;graph 仍能建,只是社区检测降级

## 概念雷达候选 1 & 2 落地(用户选「1,2」)

### 产出:两份 concept-explainer 学习资料
- `learning-materials/context-engineering.html`(17.2 KB,主题色青 `#0e7a6d`,7 段结构)
- `learning-materials/agent-memory-provenance.html`(21.8 KB,主题色玫红 `#b3426b`,7 段结构)
- 排版沿用既有 3 份的样式模板(`:root` 变量 + `section` 卡片 + `mermaid-placeholder`),保证 5 份 HTML 视觉一致

### 来源(全部 WebSearch/WebFetch 真实返回并核对标题作者)
- 上下文工程:Anthropic《Effective context engineering for AI agents》+ Claude 官方博客《new rules of context engineering》(2026-07-24)+ Anthropic《Managing context》(2025-09-30)+ Brett Sanders 51 篇工程文章综述 + Agentic AI 文档
- 长期记忆:arXiv:2608.29606《Agent Zero Memory》(2026-08-30,Ming Wu & Pengyuan Zhu)+ arXiv:2310.08560 MemGPT(Packer 2023)+ arXiv:2410.10813 LongMemEval(Wu et al., ICLR 2025)+ urandom.io / HackerNoon / Zep / AgentMarketCap 四篇二手

### 仓库索引同步
- `README.md`:目录结构图补 2 个 html;「已生成清单」补 2 行;最后更新日期 → 2026-09-16
- `concept-relationship.md`:新增「扩展:两个新概念如何接进这张网」章节(含 Mermaid),标题/配套资料链接补全
  - 关键：把「上下文工程」定位为**方法论层**(管理窗口),把「带溯源的长期记忆」定位为**时间维度**(跨会话外存);并点明「渐进披露是上下文工程的一个实例」

### LLM Wiki 二次摄取(2 source / 7 concept / 4 entity)
- raw 层:2 个 HTML 复制进 `raw/learning-materials/`(不可变)
- sources: `context-engineering.md`、`agent-memory-provenance.md`
- concepts 新增 7 个:ContextEngineering、ContextRot、Compaction、LongTermMemory、Provenance、CitationLock、Abstention
- entities 新增 4 个:MemGPT、AgentZeroMemory、LongMemEval、Zep
- 交叉链接:`ProgressiveDisclosure.md` 新增 → [[ContextEngineering]]
- index.md / overview.md 重写;log.md 追加 2 条 ingest
- **图谱重建**:17 节点/50 边 → **30 节点/115 边**(`build_graph.py --no-infer`,仍无 networkx)
- **health.py 全绿**:30 页 · Empty/Stub 0 · Index Sync 0 · Log Coverage 0

### 经验
- 概念雷达的候选落地后,「learning-materials(给人看)」与「wiki(给检索/图看)」两层要同步,否则 wiki 会相对 learning-materials 变旧
- 两份资料的来源都做到「一手 ≥ 2」:上下文工程 3 篇一手,长期记忆 3 篇一手(两个 arXiv + 一个 ICLR benchmark),符合 concept-explainer 的引用规范
- 长文件 Write 偶发被截断(本次 `agent-memory-provenance.html` 尾部断在来源第 5 条),需 `tail` 复核 + Edit 补全——写完大 HTML 后**务必校验 `</html>` 存在**
